// This is to store constants like API URLs so changing them in the future will be easy

export default function API_URL() {
	let url = "https://ec2-34-224-29-186.compute-1.amazonaws.com/"
	return url;
}
